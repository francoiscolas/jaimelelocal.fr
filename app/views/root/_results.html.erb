{
  'q': "<%=j raw @query %>",
  'l': "<%=j raw @location_name %>",
  'd': <%= @distance %>,
<% if @location_coordinates %>
  'lat': <%= @location_coordinates[0] %>,
  'lng': <%= @location_coordinates[1] %>,
<% end %>
  'farms': <%=raw @farms.to_json(only: [
    :id, :name, :url, :shortdesc, :address, :lat, :lng, :distance
   ]) %>,
}
