{
  'q': "<%=j raw @query %>",
  'l': "<%=j raw @locName %>",
  'd': <%= @distance %>,
<% if @locCoor %>
  'lat': <%= @locCoor[0] %>,
  'lng': <%= @locCoor[1] %>,
<% end %>
  'farms': <%=raw @farms.to_json(only: [
    :id, :name, :url, :shortdesc, :address, :lat, :lng, :distance
   ]) %>,
}
